# 9월 9일 운영 백업의 개발 DB 복원·검증 결과

2026-09-10 / 개발 PC 적용 완료 / 운영 DB·운영 프로그램 변경 없음

2026-09-11 후속 검증: 사용자가 확인한 ‘분할검수 체크 후 현재 회차 저장’을 WPF 요청 수집과 격리 복원본에서 추가 검증했다. 화면15개·관련 시나리오5개가 통과했다. 원본 예약 불일치가 남으면 저장은 별도로 제한되며, 정상 저장 검증은 트랜잭션 안에서만 완료 발주818의 예약을 해제한 조건이다. [상세 결과와 한계](partial-inspection-save-verification-2026-09-11.md).

## 현재 상태

운영 컴퓨터의 **2026-09-09 18:30:01 백업**을 개발 PC의 `127.0.0.1:5432 / mes_db`에 복원하고 검수 수정 마이그레이션 `5a6b7c8d9e0f`를 적용했다. 개발 API는 `http://127.0.0.1:8000`에서 실행 중이다. 기존 개발 DB와 파일 저장소는 별도 안전 백업으로 보존했고, 그 DB 백업을 실제로 복원해 복구 가능성도 확인했다.

수정 코드의 저장·수정 동작은 운영 백업의 별도 로컬 복원본에서 검증했다. 테스트의 예약 해제·수량 보정·검수 저장은 각 트랜잭션 종료 시 취소했다. 최종 개발 DB에는 원본 백업과 확정 가능한 이월 연결만 반영했다. 실물 근거가 필요한 재고 보정은 남아 있다.

| 항목 | 결과 |
|---|---|
| 운영 백업 원본 | `\\MES-SERVER\D$\mes_backup\mes_db_20260909_183001.dump` |
| 생성 시각·형식 | 2026-09-09 18:30:01, PostgreSQL 17.10 CUSTOM |
| 크기 | 817,581 bytes |
| 원본·복사본 SHA-256 | `8945cae27267fc8d0c5a0c34d77b57302b45eb47c1ac0e3ee634b1bd6357afdc` |
| 개발 DB | 로컬 `127.0.0.1:5432 / mes_db` |
| 복원 후 자료 | 발주 622행 / 검수실적 618행 / 제품 재고이동 1,439행 |
| 스키마 | `5a6b7c8d9e0f`, head 확인 및 `alembic check` 통과 |
| 실제 API 확인 | health: ok / ready: ready / 일정593·612 인증 조회: HTTP200 |
| 프로그램 | 최신 Debug 빌드, 경고0 / 오류0 |

## 실제 자료로 확인한 결과

### 일정593 / CT26H21E09

실재고65,000과 자기 예약65,000은 존재한다. 다만 완료 발주818의 예약540,000이 동일 상품의 LOT에 남아 있으므로, 수정 프로그램은 LOT 정보를 표시하면서 과다 예약을 안내하고 사용가능량을0으로 계산한다. 정상 배분값을 보내더라도 이 상태의 신규 수량 반영은409로 막는다.

개발 검증 트랜잭션에서만 발주818의 미사용 예약을 해제하면 사용가능량이65,000으로 표시된다. 이 조건에서 **재고출고65,000 + 생산출고432,000 = 출고497,000**의 분할 저장이 성공했다.

양품432,000·불량10,456의 실제 처리442,456으로 최종 종료하는 경우, 미달 사유가 없으면 차단되고 사유를 입력하면 미검수0으로 저장된다. 양품550,000이 LOT 계획535,000을 초과하는 경우도 저장되며, 출고목표에 맞춘 잔량15,000이 재고로 편입된다.

### 일정612 / 실적641

원실적412의19,500과 원실적610의20,000이 실적641에 연결되어 **이월39,500**을 보존한다. 자기 양품50,000과 기존 입고89,500의 관계가 유지된다.

- 동일값 저장: 성공. 원장 ID·수량·잔액·시각 및 출고행 ID·수량·시각 유지.
- 메모 수정: 성공. 재고 불일치가 있어도 수불을 다시 만들지 않음.
- 오래된 수정 버전으로 재요청:409 차단.
- 원본 상태의 수량 변경: 상품1160의 전체32,000/LOT82,000 불일치 때문에409 차단.
- 개발 검증에서만 전체를 LOT 합계82,000으로 맞춘 가정: 양품을55,000으로 바꾸면 입고5,000만 추가되고, 기존 출고와 이월39,500은 유지됨.

마지막 가정은 코드의 차이 반영을 검증하기 위한 입력이다. 실제 재고82,000이 맞다는 판정이나 운영 수량 정정 근거가 아니다. 이 보정은 최종 개발 DB에 남기지 않았다.

## 검증 목록

| 번호 | 검증 | 결과 |
|---|---|---|
| 1 | 593의 실재고·과다 예약 표시, 요약과 LOT 목록 기준 일치 | 통과 |
| 2 | 양품을 입력하고 배분을 누락한 요청의 수량 차이 안내 | 예상422 확인 |
| 3 | 원본 과다 예약 상태의 신규 수량 반영 | 예상409 확인 |
| 4 | 구버전 수량규칙 요청 | 변경 전409 확인 |
| 5 | 612 이월39,500 유지·동일값 저장 | 통과 |
| 6 | 612 메모 수정의 원장 보존·오래된 수정 버전 차단 | 통과 |
| 7 | 612 재고 불일치 상태의 수량 수정 | 예상409 확인 |
| 8 | 완료 발주 예약 해제 조건에서593 분할 출고497,000 | 통과 |
| 9 | 계획 미달 최종 종료에서 사유 누락 | 예상422 확인 |
| 10 | 실제442,456·미검수0·미달 사유를 기록한 최종 종료 | 통과 |
| 11 | LOT535,000 초과 양품550,000·잔량15,000 재고편입 | 통과 |
| 12 | 재고 기준 일치 가정에서612의5,000 증량을 차이만 반영 | 통과 |

총12개 시나리오가 기대 결과와 일치했다. 신규 패키지 없이 실제 FastAPI 요청 검증·응답 직렬화·저장 경로를 사용했다. 시나리오 검증에서는 테스트 사용자 의존성을 제공했으며, 최종 개발 API 조회는 실제 인증·권한 경로로 별도 확인했다. 실제 사용자의 비밀번호 로그인은 자동 검증하지 않았다.

마이그레이션 전후 및 최종 개발 DB의 다음8개 테이블을 전체 행 해시로 대조했다. 새 정산 연결·미달 사유 필드를 제외한 업무자료는 모두 원본과 일치했다.

`inspection_result`, `inspection_schedule`, `lot`, `order_line`, `product_inventory`, `product_inventory_lot`, `product_inventory_movement`, `shipment_line`

최종 개발 DB의 검수 수정 이력은0행이다. 테스트로 만든 실적·원장·예약 보정은 남아 있지 않다. 이월 연결은 `412→641(19,500)`, `610→641(20,000)`, `641→641(50,000)`으로 적용됐다.

## 추가 데이터 정리가 필요한 범위

| 점검 항목 | 건수 | 의미 |
|---|---:|---|
| 전체 재고와 LOT 합계가 다른 상품 | 20개 | 실물·LOT 증빙에 따른 정정 필요 |
| 실제 LOT 재고보다 큰 예약 | 2개 LOT | 상품1161, 발주818·844 예약 중첩 |
| 완료 발주에 남은 대기 재고출고 | 5행 | 발주482·607·818·823의 미해제 예약 점검 |
| LOT에 연결되지 않은 수동 재고이동 | 20행 | 과거 차감의 LOT 귀속 확인 필요 |
| 출고 원장과 출고 실적의 합계 차이 | 0건 | 이번 점검 기준 합계 일치 |
| 자동 복원 근거가 부족한 정산 완료 실적 | 28행 | 정산 연결 확인 전 일반 수정 제한 |
| 복원된 정산 연결의 LOT·회차·수량 오류 | 0건 | 연결 검증 통과 |

따라서 **코드 수정만 적용한 원본 자료에서는593의 신규 저장과612의 수량 변경이 계속 제한된다.** 원래의 이월 계산 오류와 별개로, 남아 있는 재고·예약 불일치를 알리는 정상적인 보호 동작이다. 612의 동일값·메모 저장은 실제 원본 상태에서 성공했다.

## 파일과 증거 보존 위치

모두 개발 PC의 Git 저장소 밖에 보존한다.

- 이전 개발 DB·파일 안전 백업: `C:\Users\jaewon\Documents\Codex\mes-v1.5-db-safety\before-20260909-production-rehearsal\mes_20260910T022842Z`
- 운영 백업 복사본·점검 결과: `C:\Users\jaewon\Documents\Codex\mes-v1.5-db-safety\production-rehearsal-20260909`
- 자료 대조: `before-migration.json`, `after-migration.json`
- 시나리오 결과: `real-data-test-results.json`
- 개발 적용·실제 API 결과: `development-applied.json`, `development-live-check.json`
- 개발 API 로그: `development-api.stdout.log`, `development-api.stderr.log`

별도 복원 검증에 사용한55439 PostgreSQL은 종료했다. 실제 개발5432 DB와8000 API는 사용 가능한 상태로 두었다. 운영 서버에서는 백업 파일 읽기·복사만 수행했다.

## 화면 확인과 복구

화면 확인은 `frontend-wpf/Mes.WpfClean/Mes.Wpf/Mes.Wpf/bin/Debug/net8.0-windows/Mes.Wpf.exe`를 사용한다. Debug 설정은 로컬8000 API에 연결한다. 이 프로젝트의 Release 설정은 운영 서버 주소를 사용하므로 이번 개발 DB 확인은 Debug 실행본 기준이다.

1. 운영 백업에 포함된 사용자 계정으로 개발용 화면에 로그인한다.
2. 593에서 LOT 실재고65,000·자기 예약·다른 예약과 점검 안내를 확인한다.
3. 612에서 이월39,500·생산출고50,000·재고편입39,500 표시를 확인한다.
4. 실제 수량 변경을 수동 시험하려면 개발 데이터에 적용할 재고 정정 가정을 먼저 명시한다. 원자료 비교와 테스트 자료를 구분해 기록한다.

이번에는 DB만 운영 백업으로 교체했다. 운영 서버의 도면·불량 사진·동판 파일은 복사하지 않았으므로 새 파일의 열기·출력은 별도 확인이 필요하다. 실제 Windows 화면 조작, 비밀번호 로그인, 납품·프린터 출력은 미검증이다.

이전 개발 상태로 복구할 때는 개발 API를 중지하고 먼저 현재 자료를 다시 보존한다. 새 검수 수정 이력·미달 사유가 없는지 확인한 뒤 호환 스키마로 되돌리고 위 안전 백업을 트랜잭션 복원한다. 새 업무 입력이 있으면 무조건 이전 전체 DB로 덮어쓰지 않고 차이를 먼저 보존·대조한다.

## 이번 작업의 변경 범위

- 변경: 로컬 개발 `mes_db`의 데이터와 검수 수정 스키마. `.env`의 DB 접속정보는 유지했다.
- 실행: 로컬 개발 API, 최신 Debug 빌드, 복원·수량·저장 검증.
- 문서: 본 보고서, 수정 결과 문서의 후속 검증 연결, 운영구조 문서의 개발 DB 현황.
- 애플리케이션 소스에는 이번 검증으로 추가 수정이 필요하지 않았다.
