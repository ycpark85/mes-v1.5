# 발주 대기목록 변경 GitHub 업로드 — 2026-09-23

서버 배포를 위한 소스를 `ycpark85/mes-v1.5` 저장소의 기존 `codex/prepare-server-deployment-20260922` 브랜치에 추가한다. 시작 커밋은 서버에 배포한 `50654e1335e0ea6e24f64597c8853847126377c8`이며, 업로드 전 원격 브랜치도 같은 커밋임을 확인했다. 이번 요청은 GitHub 소스 업로드이며 운영 서버 배포와 main 병합은 포함하지 않는다.

## 포함하는 변경

- 생산중 탭의 LOT 생성 대기·종료판단대기 목록과 전체 건수 표시.
- 현재 실적으로 완료, 수동완료 취소, 재작업에 따른 재개와 이력 기록.
- 완료 시 해당 발주의 미사용 재고 예약만 해제. 실제 출고·재고·검수 수량은 변경하지 않음.
- 처리 후 목록·선택 행·페이지·스크롤 유지와 최신 건수 갱신.
- 생산완료의 대기 건수 계산 생략, 생산중 조회 안의 대기 판단 공유. 미조회와 실제 0건 구분.
- 관련 백엔드·WPF 회귀 테스트, DB migration, 운영구조와 성능 측정 문서.

변경 파일 전체 목록은 이 업로드 커밋에 포함된다. 기능별 파일과 업무 규칙은 [대기목록 구현 기록](order-work-queues-2026-09-22.md), 최적화 파일과 전후 결과는 [성능 검증 기록](order-work-queue-performance-2026-09-22.md#후속-최적화-결과)에 정리했다.

`.env`, DB 백업·업무자료, `.tmp`의 진단·측정 자료, 가상환경, 빌드 결과와 Python 캐시는 업로드하지 않는다. 이 업로드 준비에서 애플리케이션 동작은 추가 수정하지 않았다.

## 기존 검증과 업로드 점검

2026-09-22 최종 코드 검증 결과:

- 전체 백엔드 **416 passed, 1 skipped, 112 subtests passed**. 필수 PostgreSQL 회귀 게이트 통과. 제외 1건은 Windows 테스트 계정의 심볼릭 링크 생성 권한 때문이다.
- WPF 발주 **32개 검증 통과**, 실제 컨트롤 렌더링·바인딩 오류·스크롤 유지 포함.
- WPF Release 회귀 실행과 Debug 빌드 성공. Debug 빌드 경고 0, 오류 0.
- 조회 결과 전후 비교 **100가지 사례에서 행·수량·상태·순서·검색 총건수 일치**. 생산완료 건수 응답만 의도대로 null.
- 합성 5만 발주에서 종료판단 조회 중앙값 **148.39 → 78.93ms**, 생산완료 **103.23 → 26.69ms**. 로컬 시험이며 운영 서버의 응답 시간을 보장하지 않는다.

업로드 당일은 변경 파일의 형식·누락·제외 대상과 원격 기준 커밋을 점검한다. 기능 코드에 추가 변경이 없어 전체 테스트를 반복 실행하지 않는다. 원본 결과는 로컬 `.tmp/order-performance-optimization-20260922/backend-tests.xml`과 같은 폴더의 측정 결과에 보관한다.

## 다음 서버 배포 시 적용 사항

- 서버의 예상 DB 기준은 기존 배포의 `6b7c8d9e0f1a`다. 실제 현재 revision을 확인한 후 이번 `7c8d9e0f1a2b` migration을 적용한다. GitHub 업로드 자체는 DB를 변경하지 않는다.
- migration은 발주의 LOT 보류 기원과 수동완료 여부를 추가한다. 기존 수량은 변경하지 않으며, 보류 기원 보충은 저장된 처리방식에 근거한다. 기존 수동완료/재개 이력이 생긴 뒤에는 파괴적 downgrade를 막는다.
- migration·API·새 WPF를 같은 배포에서 적용한다. 생산완료 응답의 `queue_counts: null`과 이를 처리하는 새 화면 코드를 함께 반영해야 한다.
- 배포 전 복구 백업과 실패 시 대응은 [기능 문서의 배포 계획](order-work-queues-2026-09-22.md)을 따른다. 이번 업로드에서는 운영 데이터 쓰기·서비스 재시작·WPF 교체를 하지 않는다.
- 운영 환경에서 두 대기목록 분류, 전체 버튼 숫자와 검색 결과 건수, 완료→취소→재작업, 재고 예약 해제와 실제 수량 불변, 탭 복귀·처리 후 갱신을 확인한다. 실제 서버의 성능과 장시간 동시 저장 부하는 아직 확인하지 않았다.
